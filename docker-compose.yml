services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-equipment_monitor}
      POSTGRES_USER: ${DB_USER:-equipment_monitor}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-equipment_monitor}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-equipment_monitor}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
